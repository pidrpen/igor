# Карта Mythic Key

Обновлять в том же ходе, если файл или дом фичи сдвинулся. Не дублировать журнал (`SESSION.md`) и жёлтые решения (`СЕССИЯ_разбор_баланса.md`).

Пути от папки с `AGENTS.md`. Рабочая область иногда открыта как `igor-main/` — отбросить префикс `igor-main/`. Клон `pidrpen/igor`: в корне уже игра, тот же сдвиг.

Снимок: 2026-08-16 (патч 5.4.9, кэш комнаты анимаций `5.4.9-cut2`).

## Деревья

| Путь | Роль |
|---|---|
| `AGENTS.md` | указатель на скилл |
| `.grok/skills/mythic-key/SKILL.md` | договор: Тест / перелив / новая сессия |
| `.grok/skills/mythic-key/references/карта.md` | этот файл |
| `igor-main/Тест/` | рабочая копия |
| `igor-main/` кроме `Тест/` и `прокачка/` | основа |
| `igor-main/прокачка/` | песочница паладин 1–40 |
| `igor-main/Копия не трогать/` | заморозка, не редактировать (внутри ещё старый Тест — не путать) |
| GitHub `pidrpen/igor` | public, `main`. Корень клона = содержимое `igor-main/` + `AGENTS.md` + `.grok/` |

## Документы — один дом на факт

| Вопрос | Файл |
|---|---|
| Куда писать / как переливать | `SKILL.md` |
| Генерация арта, пинг Imagine | `.grok/skills/mythic-key/references/генерация.md` |
| Где какой код | этот файл |
| Что сейчас в работе | `igor-main/Тест/SESSION.md` |
| Жёлтый разбор, как говорить, что решено | `igor-main/Тест/СЕССИЯ_разбор_баланса.md` |
| Снимок жёлтого текстом | `igor-main/Тест/задачи_мне.txt` (после новых записей пересобрать) |
| Не сейчас | `igor-main/Тест/бэклог_ветки.md` |
| Книга баланса | `igor-main/Тест/баланс_полный_обзор.xlsx` |
| Сравнение танк/хил/дд 1-5-10 | `igor-main/Тест/сравнение_ролей.xlsx` · как читать столбцы: `Тест/сравнение_ролей.md` · вход: `Тест/tools/run_rotations.py` · сборка книги: `build_role_compare.py` · честный сейв: `tank_resist.py` · честный хил: `healer_honest.py` · честный урон: `dps_honest.py` |
| Скрытые / кривые описания | `igor-main/Тест/описания_способностей.xlsx` · скрипт `Тест/tools/build_desc_audit.py` |
| Прокачка (песочница паладин) | `igor-main/прокачка/КОНЦЕПТ.md` · живая проба `igor-main/прокачка/` |
| Прокачка на Тест (ещё не код) | `igor-main/Тест/ПРОКАЧКА_на_тест.md` |
| Старый дамп кнопок | `skill_rebalance*.csv` — комментарии, не цифры |
| Стили 2D | `igor-main/Тест/предложения ИИ/README.md` |

`game-parts/` — обрезки старого монолита, не игра.

## Запуск

```bash
# основа
cd igor-main
python -m http.server 8080
# http://localhost:8080/

# тест
cd igor-main/Тест
python -m http.server 8080
# http://localhost:8080/

# прокачка — сервер из корня игры (igor-main/ или клон), иначе ../assets не найдутся
# http://localhost:8080/прокачка/
```

После JS/CSS — бамп `?v=` в том `index.html`, который менялся.

## Куда писать (Тест, пока не перелив)

| Что | Файл |
|---|---|
| Цифры кнопки / кит класса | `class-balance/<класс>-abilities.js` |
| Подключить пакет | `class-balance/apply-all.js` + тег в `index.html` |
| Искусность, `STAT_SCALE`, `FLAT_REF` | `js/state.js` |
| Открытые спеки основы / бейдж «Тест» | `js/core.js` (`PATCHED_SPECS`, `TEST_SPECS`) |
| Портреты, темы данжей, фон комнаты | `js/core.js` + `assets/` |
| Журнал баланса в лобби | `js/ui/balance-panel.js` |
| Враги, аффиксы, Лэй Шэнь | `js/enemies.js` |
| Граф ключа, силы ключа, правила цели | `js/route.js` |
| Рейд 10, ИИ союзников, провал | `js/raid.js` |
| Шмот ключа | `js/gear.js` |
| Магазин | `js/shop/` |
| Телеграф, кик, лут-силы | `js/systems/telegraph-loot.js` |
| Крит / иск / унив | `js/systems/stats.js` |
| Пассивки | `js/systems/passives.js` |
| Словарь «кавычек» в подсказке | `js/systems/ability-data.js` (`EFFECT_GLOSSARY`, `abilityDescribe`) |
| Recount | `js/systems/meter.js` |
| Петы, школы, `applyStatus`, оценка кнопки | `js/systems/ability-data.js` |
| Ресурсы, заряды, `canPay` | `js/systems/resources.js` |
| Ход / раунд | `js/systems/combat-loop.js` |
| Каст | `js/systems/abilities.js` |
| Урон, хил, блок, парир, реванш | `js/systems/damage.js` |
| ИИ врага / пета | `js/systems/ai.js` |
| Победа, привал, смена комнаты | `js/systems/combat-flow.js` |
| Карты юнитов, панель 1–9 | `js/ui/combat-ui.js` (рамка портрета при баффе/дебаффе: `has-auras`) |
| VFX | `js/ui/fx.js` |
| Лобби, отряд, старт ключа | `js/ui.js` |
| Сейв | `js/save.js` |
| Комната анимаций | `js/test-room/` · хаб + `compare.js` + `style-lab.js` + `brew-rooms.js` |
| Планы иконок/HP | `Тест/ПЛАНЫ_новая_анимация.md` |
| Реестр спрайтов | `js/test-room/sprites.js` |
| `js/combat.js` | пустышка, `index.html` не грузит |

Живые цифры — пакеты в `class-balance/`. `wow-mop-data.js` — скелет, пакет его перезаписывает.

Порядок `<script>` в `Тест/index.html`: данные и пакеты в `<head>` → `state` → `core` → UI/враги/маршрут/рейд/шмот/магазин → `systems/*` → `save` → `ui` → `test-room`. Не менять порядок без нужды. Теги `<script>` закрывать полностью.

## Только в Тесте (в основу не тащить без имени)

- `class-balance/cheat-abilities.js`
- `js/shop/`, кнопка магазина в `ui.js` / `index.html`
- `js/test-room/`, `css/test-room.css`, `assets/sprites/`
- маршрут ключа без привалов (`descent-v2` в `js/route.js`) — **уже и на основе** (с 5.4.8.42 / 5.4.9)
- `tools/*.py`, Excel, `предложения ИИ/`, `Мои пожелания/`

Инженер-пакет (`class-balance/engineer-abilities.js`) **уже в основе** после перелива 5.4.8.40. Читер остался только в Тесте.

Читер: кнопки 10–80% max HP цели, без хода.

## Уже в основе (не считать «только Тест»)

- Рейд 10, Лэй Шэнь, провал на 50%, соки кликом, метки, смена танков
- `js/raid.js`, `css/raid.css`, `assets/backgrounds/lobby-leishen.jpg`
- Иконки баффов/дебаффов слева от портрета (панель 2×6, как в Тесте)
- Фон лобби 5 ключей `lobby-key.jpg`; скрытие интерфейса и на пятёрке
- Переход ключ↔рейд молниями (вариант 2) — **Тест и основа**: `js/raid.js` `playLobbyDissolve`. Стили `#lobby-fx`: Тест — `css/test-room.css`; основа — `css/raid.css`
- Открытые в лобби: все спеки залитых классов. Охотник на демонов — серая карточка, не в `PATCHED_SPECS`

В Тесте стили рейда сидят в `css/test-room.css`. На основе — в `css/raid.css`.

## Ассеты

| Что | Путь |
|---|---|
| Фон данжа / комнаты | `assets/backgrounds/<тема>/<комната>.png` |
| Фон лобби 5 ключей | `assets/backgrounds/lobby-key.jpg` |
| Фон лобби рейда 10 | `assets/backgrounds/lobby-leishen.jpg` |
| Черновик охотника на демонов (не залит) | `Тест/class-balance/demonhunter-abilities.js` — карточка серая, нет в `PATCHED_SPECS` |
| Портрет класса / спека / врага / пета | `assets/portraits/...` |
| 2D (только Тест) | `Тест/assets/sprites/characters/<id>/` |

Темы: `crypt` `forge` `tide` `jade` `rift` `ember`. Комнаты основы: `entrance` `corridor` `gallery` `elite` `mid` `rest` `throne` `mopup` `depths`. В Тесте у тем могут быть ещё `inner` `sanctum` `approach` `annex`.

Спрайт-паки сейчас: злой дух; паладин Защита (по скиллам); паладин Воздаяние (крылья); шаман Исцеление (по скиллам). Сравнение стилей (не реестр боя): `characters/mage_frost_compare/` + `fx/frostbolt.png`. Хмелевар 8-bit в Тест анимаций: `characters/monk_brew_nes/` (idle, jab, keg, breath, kick) + `fx/keg_fly.png` / `keg_shard_0–4` / `fire_jet.png` / `fire_burst.png`. Остальные спеки в арене — портрет «скоро». `manifest.json` комната не читает.

## Прокачка

`igor-main/прокачка/` — свой `index.html` + `js/data.js` + `js/engine.js` + `css/proto.css`. Сейв `igorLevelingPaladin_v1`. Цифры паладина скопированы из кита, движок ключа не подключает. Перелив — только по слову, сначала не в корень.

Как это сядет в Тест (сейв `igorHero_v1`, таланты от таблицы ролей, без вшивки в ключ) — `Тест/ПРОКАЧКА_на_тест.md`. Не пилить, пока пользователь не сказал «делай».

## Константы — смотреть в файле, не запоминать

| Символ | Файл |
|---|---|
| `STAT_SCALE` (1000), `FLAT_REF` (15) | `js/state.js` |
| `MASTERY_BY_SPEC` | `js/state.js` |
| `PATCHED_SPECS` / `TEST_SPECS` | `js/core.js` |
| `TALENTS` (силы ключа, не дерево героя) | `js/route.js` |
| `AFFIXES`, `ENEMIES` | `js/enemies.js` |
| `applyStatus` (дот/хот/уязв. по id+fromUid; личные баффы по id) | `js/systems/ability-data.js` |
| Броня (сейчас вычитание защиты, жёлтое хочет %) | `js/systems/damage.js` `dealDmg` |

Скорость только сортирует очередь. `перезарядка 1` = кнопка готова каждый твой ход. Энергия Света каждый пулл = 3.

## Баланс-конвейер

`class-balance/*.js` → `apply-all.js` → то, во что играют.

Обзор: `Тест/tools/build_full_review.py` → `баланс_полный_обзор.xlsx`. Колонки ресурса: `patch_resource_review.py`.

Разбор: агент разжёвывает строку → пользователь диктует задачу → писать в Excel. Код не трогать, пока не попросили вшить. Жёлтое по классам залито до инженера S35. S36 сапёр забит. S37 не трогать.

Лист «Кнопки» считает голые т без ресурса — не резать трату только потому что цифра больше копилки.

## Тесты

`igor-main/tests/` и `Тест/tests/` — дым, не бой. `dot-per-caster-smoke.js`, `engine-sheet-smoke.js`. `load-check.js` всё ещё ищет `startCombat` в пустышке `js/combat.js` (протухло). Тестовый smoke ещё требует инженера.

## Что не читать «чтобы войти в курс»

- `Копия не трогать/`
- весь `class-balance/` подряд
- Excel целиком
- `предложения ИИ/_work_*`
- прошлые сессионные логи агента
