# Сессия — тестовая комната / пиксельные анимации

**Дата:** 2026-08-08  
**Рабочая копия:** `новое серьезно/Тест/`  
**Оригинал игры:** `новое серьезно/` (не трогали, работаем только в `Тест`)

---

## Что сделано

1. Создана папка **`Тест`** — полная копия игры (~207 файлов) для экспериментов со спрайтами.
2. Добавлена **тестовая комната анимаций**:
   - Лобби → кнопка **«Тестовая комната (анимации)»**
   - Список всех специализаций → выбор → **Запустить комнату**
   - Арена: герой слева · **Злой дух** справа (враг по умолчанию)
   - Клавиши: `1` герой · `2` враг · `3` оба · `0`/`I` idle · `Esc` к списку
3. Пиксельные спрайты (48×48 → ×6 nearest, `image-rendering: pixelated`):
   - `assets/sprites/characters/evil_spirit/` — idle×8, attack×6
   - `assets/sprites/characters/paladin_protection/` — idle×8, attack×6
4. Остальные спеки в списке — заглушка (портрет), бейдж «скоро».

---

## Ключевые файлы

| Путь | Назначение |
|------|------------|
| `index.html` | Экраны `#test-picker`, `#test-arena` + подключение CSS/JS |
| `css/test-room.css` | UI комнаты + pixelated rendering |
| `js/test-room/sprites.js` | `SPRITE_PACKS`, проигрыватель кадров |
| `js/test-room/test-room.js` | Пикер спеков, арена, хоткеи |
| `assets/sprites/manifest.json` | Манифест паков |
| `assets/sprites/characters/*` | PNG-кадры |
| `tools/gen_pixel_sprites.py` | Генератор пиксельных placeholder-спрайтов |
| `tools/gen_test_sprites.py` | Старый гладкий генератор (не использовать) |

Версии query: `?v=5.4.8.34-tr2` (при правках бампить, иначе кэш).

---

## Запуск

```bash
cd "Тест"
python -m http.server 8080
# http://localhost:8080/
```

Перегенерация пикселей:

```bash
python tools/gen_pixel_sprites.py
```

---

## Как добавить новый спрайт

1. Папка `assets/sprites/characters/<id>/` с кадрами `idle_00.png`… `attack_00.png`…
2. Запись в `SPRITE_PACKS` в `js/test-room/sprites.js` с ключом `classId:specId`
3. Обновить `manifest.json` по желанию

---

## Контекст / договорённости

- Работаем **только в `Тест`**, основной билд не ломаем.
- Цель дальше: **2D пиксельные** анимации персонажей и ударов (не гладкий art, не только портреты).
- Сейчас черновые пиксельные болванки, чтобы крутился пайплайн арены.
- Дефолт-враг: **evil_spirit**. Первый герой со спрайтом: **paladin:protection**.

---

## Стиль-борды (после рефа из «Мои пожелания»)

Папка **`Тест/предложения ИИ/`** — 8 вариаций 2D-стиля (паладин + дух):
`01_chibi` … `08_flat_vector` + `README.md`.  
Реф: `Мои пожелания/Снимок экрана…png`.

### Выбрано и внедрено: **02 · 16-bit SNES**
- Сборка: `tools/build_v02_anims.py`
- Кадры: `assets/sprites/characters/{paladin_retribution,evil_spirit}/`
- Герой с анимациями: **только paladin:retribution**
- Idle **статичный без крыльев**
- **Гнев карателя**: `wing_in` / `wing_out`; длительность = **buffTurns** (ходы), не таймер
- Ход крыльев жрётся скиллом **без** freeAction; freeAction не тикает
- Панель способностей = классы `.ability` / `.ability-bar` как в бою
- Сборка кадров: `tools/build_paladin_anims_v3.py`
- **TEST_SPECS** в `js/core.js` — разблокированные «Тест» спеки (бейдж в лобби)
- Полный ребаланс class-balance/* для всех ранее locked спеков (суббагенты)
- **Магазин** (FAB справа сверху): сеты +2/+8/+12/+15, trinkets по спекам, общая сумка `mythicKeySharedBag_v1`
- Шмот: «Авто-одеть» из общей сумки; `js/shop/*`
- **Баланс классов β**: changelog beta.1–4; бейдж «β Бета»; при вкладке «1. Класс» фильтр сбрасывается на «Все»

## Что логично делать после перезагрузки

- [ ] Выбрать стиль из `предложения ИИ/` (номер 01–08)
- [ ] Проверить комнату в браузере (Ctrl+F5)
- [ ] Нарезать idle/attack спрайты в выбранном стиле
- [ ] Добавить спрайты других спеков
- [ ] Привязать анимации ударов к abilities

---

## Промпт для продолжения

> Продолжи работу в `Тест`: тестовая комната анимаций уже есть (см. SESSION.md). Пиксельные спрайты evil_spirit + paladin_protection, пайплайн в js/test-room/. Дальше — …
